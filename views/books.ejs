<!DOCTYPE html>
<html>
  <head>
    <title>Node App</title>
  </head>
  <body>
    <h1>Books</h1>
    <ul>
      <% books.forEach(function(book) { %>
      <li><%= book.title %> by <%= book.author %></li>
      <% }) %>
    </ul>
  </body>
</html>